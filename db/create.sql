create table items (id serial primary key, name text);
create table cars(
                     id serial primary key,
                     model varchar(255),
                     created timestamp
);